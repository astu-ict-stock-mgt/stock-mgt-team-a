{
  "name": "stock-management-system",
  "version": "1.0.0",
  "description": "Full-stack Stock Management System",
  "private": true,
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
